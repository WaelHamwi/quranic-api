; PHP per-directory overrides — read by PHP-FPM automatically.
; Allows uploading audio recordings up to 200 MB via Filament admin.
upload_max_filesize = 200M
post_max_size = 210M
max_execution_time = 300
max_input_time = 300
memory_limit = 256M
